import { FormGroup } from './form-group'
import { FormItem } from './form-item'
import { FormRoot } from './form-root'

export const Form = {
  Root: FormRoot,
  Group: FormGroup,
  Item: FormItem,
}
