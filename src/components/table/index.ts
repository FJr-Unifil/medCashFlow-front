import { TableBody } from "./table-body";
import { TableHead } from "./table-head";
import { TableRoot } from "./table-root";

export const Table = {
  Root: TableRoot,
  Head: TableHead,
  Body: TableBody
}